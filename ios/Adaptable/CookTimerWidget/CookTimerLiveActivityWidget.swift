import WidgetKit
import SwiftUI
import ActivityKit

/// Moved into the compiled `Widgets/` target. Kept here only so this folder
/// does not look like the shipping copy. See `Widgets/CookTimerLiveActivityWidget.swift`.
enum CookTimerWidgetFolderMoved {
    static let shippingPath = "ios/Adaptable/Widgets/CookTimerLiveActivityWidget.swift"
}
